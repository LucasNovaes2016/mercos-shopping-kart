export interface IHeaderInfo {
  numero: string;
  usuario: string;
  local: string;
}

export interface IHeaderMenu {
  cod: number;
  name: string;
  brand: string;
  description: string;
  price: number;
}
